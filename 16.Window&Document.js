// Window
// 1. Window is the main container or global object and any operations related to entire browser window can be part of window object 
// 2. All members like objects, methods or properties, if they are part of window object then we do not refer to window object
// 3. Windows has methods, properties and object, ex setTimeout() or setInterval() are the methods, whereas DOCUMENT is the object of WINDOW and it also has a screen object with properties describing the physical display

// Document
// 1. DOM is the child of window object
// 2. Where in the DOM we need to refer the document, if we want to use the document object, method or properties
// 3. Document is just the object of the global object that is Window, which deals with the document, the HTML elements themselves